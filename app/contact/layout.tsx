export const metadata = {
  title: "Contact | IME Digital Solutions",
  description: "Request a demo or contact IME Digital Solutions.",
};

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
